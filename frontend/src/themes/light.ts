const light = {
  title: 'light',
  colors: {
    background: `var(--lightBackground)`,
    line: `var(--lightLine)`,
    text: `var(--lightText)`,
    textHighlight: `var(--lightTextHighlight)`,
    title: `var(--lightTitle)`,
  }
}

export default light;
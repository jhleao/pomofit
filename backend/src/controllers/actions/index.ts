import { Controller } from '@types';

export const start: Controller = (req, res) => {

  return res.sendStatus(200);
};

export const cancel: Controller = (req, res) => {

  return res.sendStatus(200);
};
